ONE-FILE PROJX RACING PREVIEW

Open index.html directly for the interactive preview.

For a temporary GitHub Pages preview, upload index.html to an empty repository root, then enable GitHub Pages from main / (root).

Use the separate GitHub Web Upload package for the preferred production-style file structure and faster loading.
